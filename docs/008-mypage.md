# 008 マイページ

## 概要

ログインユーザーが自分の学習中の講座一覧と進捗を確認できるページ。ログイン必須。

## 関連

- 機能ID: F-07
- 画面ID: P-05（`/my`）
- Phase: 5
- 推定: 0.5日

## TODO

### データ取得
- [x] `getClaims()` でログイン中のユーザーを取得する
- [x] `progress` テーブルから `user_id` が一致するレコードを取得する
- [x] progress を持つ講座（`courses`）と各講座のレッスン数・視聴済み数を集計する
- [x] 未ログインの場合は `/login` にリダイレクトする（proxy でも保護しているが念のため）

### UI
- [x] `app/my/page.tsx` を実装する
  - ユーザーのアバター・表示名
  - 学習中の講座一覧（`CourseCard` を流用、進捗率を追加表示）
  - 進捗率が 100% の講座は「完了」バッジを表示する
  - 学習中の講座が0件の場合は「まだ学習中の講座がありません」と表示し、講座一覧へ誘導する

### ローディング
- [x] `app/my/loading.tsx` を作成しスケルトン UI を表示する

### SEO
- [x] `generateMetadata` でページタイトルを設定する（`noindex` 推奨）
