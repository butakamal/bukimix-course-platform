# 011 GA4 アナリティクス

## 概要

Google Analytics 4 を導入し、ページビューや講座閲覧などの基本的な計測を行う。

## 関連

- 機能ID: F-11
- Phase: 6（デプロイ前に対応）
- 推定: 0.5日未満

## TODO

### GA4 プロパティ
- [ ] Google Analytics で GA4 プロパティを作成する
- [ ] 計測 ID（`G-XXXXXXXXXX`）を取得する
- [x] 計測 ID を環境変数 `NEXT_PUBLIC_GA_MEASUREMENT_ID` に設定する（本番のみ値を入れる）

### 実装
- [x] GA4 スクリプトを `app/layout.tsx` に組み込む
  - `next/script` の `strategy="afterInteractive"` で読み込む
  - 計測 ID を環境変数から渡す
- [ ] ページ遷移ごとに `page_view` イベントが送信されることを確認する

### 動作確認
- [ ] GA4 リアルタイムレポートでページビューが計測されていることを確認する
- [ ] ブラウザの開発者ツールで GA4 リクエストが送信されていることを確認する

## 注意事項

- プライバシーポリシーに GA4 の利用を明記する（チケット010と連携）
- 本番デプロイ後に計測を開始する（ローカル環境では誤計測しないよう `GA_MEASUREMENT_ID` を本番のみ設定）
