# 009 トップページ

## 概要

サービスの紹介と講座一覧への導線となるランディングページ。ゲストでもアクセス可能。

## 関連

- 画面ID: P-01（`/`）
- Phase: 5
- 推定: 0.5日

## TODO

### コンテンツ
- [x] ヒーローセクションを実装する
  - キャッチコピー・サービス説明文
  - 「講座を見る」CTA ボタン（`/courses` へ）
  - ログイン状態によって CTA を切り替える（未ログイン: 「無料で始める」、ログイン済み: 「マイページへ」）
- [x] 人気講座セクション（または全講座の先頭数件）を表示する
  - `CourseCard` コンポーネントを流用する
- [x] サービスの特徴セクション（例: 体系的に学べる・無料・スマホ対応）を実装する

### UI / レイアウト
- [x] `app/page.tsx` を実装する
- [x] グローバルヘッダーを `app/layout.tsx` に配置する
  - ロゴ / 講座一覧リンク / マイページリンク / ログインボタン
- [x] グローバルフッターを `app/layout.tsx` に配置する
  - 利用規約・プライバシーポリシーへのリンク

### キャッシュ
- [x] 人気講座データは `'use cache'` + `cacheLife('hours')` でキャッシュする

### SEO
- [x] `app/layout.tsx` の `metadata` でサービス名・ディスクリプション・OGPを設定する
