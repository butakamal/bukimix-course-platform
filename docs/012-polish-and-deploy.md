# 012 仕上げ・本番デプロイ

## 概要

レスポンシブ対応・エラーページ・OGP などの最終調整を行い、Vercel 本番環境にデプロイする。

## 関連

- 機能ID: F-08
- 画面ID: P-07（エラーページ）
- Phase: 6

## TODO

### レスポンシブ対応（F-08）
- [ ] スマホ（375px）で全ページのレイアウト崩れがないことを確認する
- [ ] PC（1280px）で全ページのレイアウトを確認する
- [ ] タブレット（768px）での表示を確認する
- [ ] `CourseCard` グリッドがブレークポイントで正しく切り替わることを確認する
- [ ] レッスン視聴ページの動画が 16:9 を維持することを確認する
- [x] ヘッダーのモバイル用ハンバーガーメニューを実装する

### エラーページ（P-07）
- [x] `app/not-found.tsx` を作成する（グローバル 404）
- [x] `app/error.tsx` を作成する（グローバルエラー境界、`'use client'` 必須）
- [x] `app/global-error.tsx` を作成する（ルートレイアウト外のエラー用、`<html>` / `<body>` 必須）

### アセット
- [ ] favicon を設置する（`app/favicon.ico` 差し替え）
- [ ] OGP 画像を作成・設置する（`public/og-image.png`、推奨サイズ: 1200×630px）
- [x] `app/layout.tsx` の `metadata.openGraph` を設定する

### 受け入れ基準の最終確認
- [ ] ゲストで講座一覧と講座詳細が閲覧できる
- [ ] Google ログイン・ログアウトができる
- [ ] ログイン後にレッスン視聴ページで YouTube 動画が再生できる
- [ ] 視聴済みボタンを押すと再読込後も状態が保持される
- [ ] 動画を最後まで観ると自動で視聴済みになる
- [ ] 講座詳細ページに「X/Y本（Z%）」形式の進捗率が表示される
- [ ] マイページに学習中の講座が表示される
- [ ] スマホ（375px）でレイアウトが崩れない

### 本番デプロイ
- [ ] Vercel の環境変数がすべて設定されていることを確認する
- [x] `npm run build` がエラーなく完了することを確認する
- [x] `npm run lint` がエラーなく通ることを確認する
- [ ] 本番 URL で受け入れ基準をすべて通過する
- [ ] GA4 でアクセス計測が始まっていることを確認する
